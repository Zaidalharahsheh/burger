<h1>Feed Me !!!</h1>

<h2> devoured burgers !</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}

  {{/unless}}
  {{/each}}
</ul>

<h2>burgers that are ready!</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  {{/if}}
  {{/each}}
</ul>